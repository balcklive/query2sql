# Database Configuration
# PostgreSQL connection URL format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://hirelala:your_password@localhost:5432/your_database

# Query Security Settings
# Maximum number of rows returned by any query (default: 100)
MAX_QUERY_RESULTS=100

# LLM Configuration (Optional)
# Only needed for standalone testing or when MCP client doesn't provide LLM
# When running with Claude Desktop, these are NOT needed (ctx.sample uses Claude)
# OPENAI_API_KEY=sk-your-api-key-here
# OPENAI_MODEL=gpt-4
# OPENAI_BASE_URL=https://api.openai.com/v1
